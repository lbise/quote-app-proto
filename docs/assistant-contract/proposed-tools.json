[
  {
    "name": "edit_quote_details",
    "description": "Edit the current Working Draft's reference, project title, dates, work-site address, Customer and business details, terms, VAT registration and identifier, or discount. Include only fields to change. Use an empty string to clear a text or decimal field. Do not supply calculated totals, currency or VAT rates.",
    "parameters": {
      "type": "object",
      "properties": {
        "fields": {
          "type": "object",
          "properties": {
            "reference": {
              "type": "string",
              "maxLength": 200
            },
            "title": {
              "type": "string",
              "maxLength": 20000
            },
            "issueDate": {
              "type": "string",
              "maxLength": 10,
              "description": "YYYY-MM-DD or empty."
            },
            "validUntil": {
              "type": "string",
              "maxLength": 10,
              "description": "YYYY-MM-DD or empty."
            },
            "siteAddress": {
              "type": "string",
              "maxLength": 20000
            },
            "customerName": {
              "type": "string",
              "maxLength": 20000
            },
            "customerAddress": {
              "type": "string",
              "maxLength": 20000
            },
            "customerContact": {
              "type": "string",
              "maxLength": 20000
            },
            "businessName": {
              "type": "string",
              "maxLength": 20000
            },
            "businessAddress": {
              "type": "string",
              "maxLength": 20000
            },
            "businessContact": {
              "type": "string",
              "maxLength": 20000
            },
            "terms": {
              "type": "string",
              "maxLength": 20000
            },
            "vatRegistered": {
              "anyOf": [
                {
                  "type": "boolean"
                },
                {
                  "type": "null"
                }
              ],
              "description": "True applies the supported standard VAT treatment, false means not registered, null means unknown. Never infer registration."
            },
            "vatId": {
              "type": "string",
              "maxLength": 20000
            },
            "discountMode": {
              "type": "string",
              "enum": [
                "none",
                "percent",
                "fixed"
              ]
            },
            "discount": {
              "type": "string",
              "maxLength": 20,
              "pattern": "^$|^[0-9]+([.,][0-9]+)?$",
              "description": "Decimal without units, or the empty string to deliberately clear the value. Missing is not zero."
            }
          },
          "required": [],
          "additionalProperties": false,
          "minProperties": 1
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "fields": {
                "type": "array",
                "items": {
                  "type": "string",
                  "maxLength": 160,
                  "minLength": 1
                },
                "minItems": 1,
                "maxItems": 200,
                "uniqueItems": true,
                "description": "Fields supported by this citation. For edit_quote_details, use field names such as discountMode and discount. For other tools, use JSON Pointers such as /sections/0/title."
              },
              "source": {
                "type": "string",
                "maxLength": 256,
                "minLength": 1,
                "description": "Source supplied by the application: current, history_N, quote.FIELD, line:ID.FIELD or section:ID.FIELD. Never cite an assistant message."
              },
              "text": {
                "type": "string",
                "maxLength": 2000,
                "minLength": 1,
                "description": "Exact excerpt from the identified source. One citation may support several fields."
              }
            },
            "required": [
              "fields",
              "source",
              "text"
            ],
            "additionalProperties": false
          },
          "minItems": 1,
          "maxItems": 200,
          "description": "Cite sources for new nonempty commercial facts. Omit for deliberate clearing or unchanged values."
        }
      },
      "required": [
        "fields"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "edit_quote_lines",
    "description": "Create or edit up to 50 Quote Lines in one call. Send every line field: include an existing ID to edit it, or omit ID to create it. Preserve unchanged values and use empty strings only for unknown, cleared or mode-incompatible values. For evidence, group supported JSON Pointer fields under one exact excerpt from current, history_N or an original-draft source. Every new line or changed mode needs /lines/N/mode; every changed nonempty description, quantity, unit, unitPrice or amount needs evidence. Do not cite unchanged values, cleared values or structural IDs. Do not copy, move or delete lines with this tool.",
    "parameters": {
      "type": "object",
      "required": [
        "lines"
      ],
      "properties": {
        "lines": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "description",
              "mode",
              "quantity",
              "unit",
              "unitPrice",
              "amount"
            ],
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1,
                "maxLength": 128,
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
              },
              "sectionId": {
                "type": "string",
                "maxLength": 128,
                "description": "For new lines only: destination section ID. Omit or use an empty string for No section."
              },
              "description": {
                "type": "string",
                "maxLength": 20000,
                "description": "The complete current work description. Cite it when a new or changed nonempty description is supplied."
              },
              "mode": {
                "anyOf": [
                  {
                    "type": "string",
                    "const": "quantity"
                  },
                  {
                    "type": "string",
                    "const": "fixed"
                  }
                ],
                "description": "quantity uses quantity, unit and unitPrice. fixed uses amount. Cite /lines/N/mode for every new line or changed mode."
              },
              "quantity": {
                "type": "string",
                "maxLength": 20,
                "pattern": "^$|^[0-9]+([.,][0-9]+)?$",
                "description": "Quantity without a unit. Use only with quantity mode; otherwise send an empty string. Cite /lines/N/quantity when changed and nonempty."
              },
              "unit": {
                "type": "string",
                "maxLength": 100,
                "description": "Unit for quantity mode, such as m² or h. Send an empty string for fixed mode. Cite /lines/N/unit when changed and nonempty."
              },
              "unitPrice": {
                "type": "string",
                "maxLength": 20,
                "pattern": "^$|^[0-9]+([.,][0-9]+)?$",
                "description": "Unit price without currency. Use only with quantity mode; otherwise send an empty string. Cite /lines/N/unitPrice when changed and nonempty."
              },
              "amount": {
                "type": "string",
                "maxLength": 20,
                "pattern": "^$|^[0-9]+([.,][0-9]+)?$",
                "description": "Fixed amount without currency. Use only with fixed mode; otherwise send an empty string. Cite /lines/N/amount when changed and nonempty."
              }
            },
            "additionalProperties": false,
            "description": "Complete line content. Send every field on every create or edit. Quantity pricing uses quantity, unit and unitPrice and leaves amount empty. Fixed pricing uses amount and leaves quantity, unit and unitPrice empty. Cite each changed nonempty commercial value."
          },
          "minItems": 1,
          "maxItems": 50
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "object",
            "required": [
              "fields",
              "source",
              "text"
            ],
            "properties": {
              "fields": {
                "type": "array",
                "items": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 160
                },
                "minItems": 1,
                "maxItems": 200,
                "uniqueItems": true,
                "description": "Line JSON Pointer targets supported by this citation, such as /lines/0/quantity and /lines/0/unitPrice. One citation may name every field supported by its excerpt."
              },
              "source": {
                "type": "string",
                "minLength": 1,
                "maxLength": 256,
                "description": "Application-supplied source only: current, history_N, quote.FIELD, line:ID.FIELD or section:ID.FIELD. Never cite an assistant message."
              },
              "text": {
                "type": "string",
                "minLength": 1,
                "maxLength": 2000,
                "description": "Exact literal excerpt from source. Do not paraphrase it. One citation may support several fields."
              }
            },
            "additionalProperties": false
          },
          "minItems": 1,
          "maxItems": 200,
          "description": "Grouped citations. Cite /lines/N/mode for every new line or changed mode, and every changed nonempty description, quantity, unit, unitPrice or amount. No citation is needed for unchanged values, cleared values or structural IDs."
        }
      },
      "additionalProperties": false
    }
  },
  {
    "name": "edit_quote_sections",
    "description": "Create or rename up to 50 Quote Sections in one call. Supply a title for each section. Include its existing ID to rename it; omit the ID to create a new section. Use an empty title to clear it. Cite evidence for every new or changed nonempty title using /sections/0/title, /sections/1/title, etc. The Artisan's work or room description can support a faithfully reworded title. Do not add, edit, move, copy or delete Quote Lines with this tool. Do not move, copy or delete sections with this tool.",
    "parameters": {
      "type": "object",
      "properties": {
        "sections": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "id": {
                "type": "string",
                "minLength": 1,
                "maxLength": 128,
                "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
              },
              "title": {
                "type": "string",
                "maxLength": 4000
              }
            },
            "required": [
              "title"
            ],
            "additionalProperties": false
          },
          "minItems": 1,
          "maxItems": 50
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "fields": {
                "type": "array",
                "items": {
                  "type": "string",
                  "maxLength": 160,
                  "minLength": 1
                },
                "minItems": 1,
                "maxItems": 200,
                "uniqueItems": true,
                "description": "Fields supported by this citation. For edit_quote_details, use field names such as discountMode and discount. For other tools, use JSON Pointers such as /sections/0/title."
              },
              "source": {
                "type": "string",
                "maxLength": 256,
                "minLength": 1,
                "description": "Source supplied by the application: current, history_N, quote.FIELD, line:ID.FIELD or section:ID.FIELD. Never cite an assistant message."
              },
              "text": {
                "type": "string",
                "maxLength": 2000,
                "minLength": 1,
                "description": "Exact excerpt from the identified source. One citation may support several fields."
              }
            },
            "required": [
              "fields",
              "source",
              "text"
            ],
            "additionalProperties": false
          },
          "minItems": 1,
          "maxItems": 200,
          "description": "Required for every new or changed nonempty title. Cite /sections/0/title, /sections/1/title, etc., using the Artisan's work or room description. Omit only for unchanged or cleared titles."
        }
      },
      "required": [
        "sections"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "copy_quote_work",
    "description": "Copy up to 50 Quote Lines or one Quote Section with its lines. For line copies, optionally choose a destination section; otherwise copies follow their source lines. For a section copy, supply its title. Set measurementPolicy to retain to keep measurements, or unknown to clear quantities and remove embedded measurements. Other values are retained.",
    "parameters": {
      "type": "object",
      "properties": {
        "source": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "lineIds": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 128,
                    "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
                  },
                  "minItems": 1,
                  "maxItems": 50,
                  "uniqueItems": true
                },
                "destinationSectionId": {
                  "type": "string",
                  "maxLength": 128,
                  "description": "Existing Quote Section ID, or the empty string for No section."
                }
              },
              "required": [
                "lineIds"
              ],
              "additionalProperties": false
            },
            {
              "type": "object",
              "properties": {
                "sectionId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 128,
                  "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
                },
                "title": {
                  "type": "string",
                  "maxLength": 4000,
                  "minLength": 1
                }
              },
              "required": [
                "sectionId",
                "title"
              ],
              "additionalProperties": false
            }
          ]
        },
        "measurementPolicy": {
          "type": "string",
          "enum": [
            "retain",
            "unknown"
          ]
        },
        "evidence": {
          "type": "array",
          "items": {
            "type": "object",
            "properties": {
              "fields": {
                "type": "array",
                "items": {
                  "type": "string",
                  "maxLength": 160,
                  "minLength": 1
                },
                "minItems": 1,
                "maxItems": 200,
                "uniqueItems": true,
                "description": "Fields supported by this citation. For edit_quote_details, use field names such as discountMode and discount. For other tools, use JSON Pointers such as /sections/0/title."
              },
              "source": {
                "type": "string",
                "maxLength": 256,
                "minLength": 1,
                "description": "Source supplied by the application: current, history_N, quote.FIELD, line:ID.FIELD or section:ID.FIELD. Never cite an assistant message."
              },
              "text": {
                "type": "string",
                "maxLength": 2000,
                "minLength": 1,
                "description": "Exact excerpt from the identified source. One citation may support several fields."
              }
            },
            "required": [
              "fields",
              "source",
              "text"
            ],
            "additionalProperties": false
          },
          "minItems": 1,
          "maxItems": 200,
          "description": "Cite the supplied section title with /source/title when copying a section. Line copies only need evidence when they introduce a new nonempty commercial fact; omit for unchanged values or deliberate clearing."
        }
      },
      "required": [
        "source",
        "measurementPolicy"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "move_quote_work",
    "description": "Move or reorder up to 50 Quote Lines or Quote Sections. For lines, supply a destination section ID; use an empty string for No section. Supply IDs in the desired order. Use beforeLineId or beforeSectionId to insert before an existing line or section; omit it to append. Do not edit content, copy or delete work with this tool.",
    "parameters": {
      "type": "object",
      "properties": {
        "move": {
          "anyOf": [
            {
              "type": "object",
              "properties": {
                "lineIds": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 128,
                    "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
                  },
                  "minItems": 1,
                  "maxItems": 50,
                  "uniqueItems": true
                },
                "destinationSectionId": {
                  "type": "string",
                  "maxLength": 128,
                  "description": "Existing Quote Section ID, or the empty string for No section."
                },
                "beforeLineId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 128,
                  "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
                }
              },
              "required": [
                "lineIds",
                "destinationSectionId"
              ],
              "additionalProperties": false
            },
            {
              "type": "object",
              "properties": {
                "sectionIds": {
                  "type": "array",
                  "items": {
                    "type": "string",
                    "minLength": 1,
                    "maxLength": 128,
                    "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
                  },
                  "minItems": 1,
                  "maxItems": 50,
                  "uniqueItems": true
                },
                "beforeSectionId": {
                  "type": "string",
                  "minLength": 1,
                  "maxLength": 128,
                  "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
                }
              },
              "required": [
                "sectionIds"
              ],
              "additionalProperties": false
            }
          ]
        }
      },
      "required": [
        "move"
      ],
      "additionalProperties": false
    }
  },
  {
    "name": "delete_quote_lines",
    "description": "Delete up to 50 explicitly identified Quote Lines. Supply their existing IDs. Do not delete Quote Sections or remove all work; direct those requests to the manual controls.",
    "parameters": {
      "type": "object",
      "properties": {
        "lineIds": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1,
            "maxLength": 128,
            "pattern": "^[A-Za-z0-9][A-Za-z0-9_-]*$"
          },
          "minItems": 1,
          "maxItems": 50,
          "uniqueItems": true
        }
      },
      "required": [
        "lineIds"
      ],
      "additionalProperties": false
    }
  }
]
